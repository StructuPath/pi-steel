# Synthetic RFQ fixtures

Every fixture in this directory was authored from scratch for pi-steel tests.
Names use the public-policy placeholders, identifiers are prefixed with
`SYNTHETIC-`, and no file is derived from an operating company, vendor quote,
customer estimate, purchasing terms, or production workbook.

The profile's approved terms are explicitly labeled synthetic test text. The
fixture contains weights but no vendor prices or commercial rates.
